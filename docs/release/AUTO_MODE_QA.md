# Auto Mode QA

Entitlement rules enable Auto Mode for Plus and Pro and disable it for Free. The UI/server contracts compile.

Auto Mode is not release-verified until a supported live connector imports newly graded work, respects the configured time window, deduplicates events, survives token expiry, produces a notification, and never sends an appeal without confirmation. Mark unavailable connectors honestly.
