# Android build QA

Capacitor Android sync succeeds and current web assets are copied. Native build and emulator/device launch are **blocked** because Java 17+ and the required Android toolchain are unavailable on this machine.

Required: signed internal-test AAB/APK, file/camera permissions, back button, OAuth/deep links, notifications, pause/resume, offline behavior, billing, PDF rendering, and multiple screen densities. No store-size claim is made without artifacts.
