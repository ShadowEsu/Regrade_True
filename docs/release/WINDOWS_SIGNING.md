# Windows signing

No Windows installer was verified on Windows. Release requires a Windows build runner, stable publisher identity, Authenticode certificate, signed installer and binaries, SmartScreen/reputation testing, install/upgrade/uninstall tests, protocol callback verification, and malware scanning. Cross-build output alone is not sufficient evidence.
