# Notification QA

The app contains notification preferences and local-notification native wiring. Capacitor sync succeeds.

Required real-device tests: initial permission, denial, later enablement in OS settings, quiet hours, grouped messages, badge clearing, each category toggle, deep links to the correct learner/case, parent notifications, and logout/account-switch behavior. No push provider production credential was exercised.
