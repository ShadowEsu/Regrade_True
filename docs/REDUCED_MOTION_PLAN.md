# Reduced Motion Plan

When reduced motion is enabled, Regrade removes translation, parallax, repeated pulsing, count-up interpolation, spring overshoot, stagger, and auto-scrolling. Content renders at final values and panels change with short opacity only. No information, progress, or affordance depends on animation. Native and browser reduced-motion settings must be verified in the real-device plan.
