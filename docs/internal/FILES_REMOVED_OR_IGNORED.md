# Files removed or ignored

Removed from tracking in the 2026-07-12 pass: the obsolete standing-agent brief that
contained a personal workstation path. Existing documentation was grouped under the
public `docs/` taxonomy.

That brief still exists in the local repository's initial Git history. Do not publish the
full history until it is squashed or intentionally rewritten; this pass did not perform a
destructive history rewrite.

Ignored: real `.env` files, Firebase Admin/service-account files, OAuth/Apple/Android
credentials, keystores, local native config, logs, databases, uploads, caches, builds,
test reports, temporary screenshots, deliverables, design backups, and private local notes.

Ignored files may remain on a developer machine. Their presence does not mean they are
safe to copy, publish, attach to issues, or include in support bundles.
