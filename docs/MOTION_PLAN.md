# Motion Plan

Implement motion through shared primitives rather than per-screen CSS: page transition, staggered reveal, press feedback, shared card/detail layout IDs, spring sheet, count-up value, progress stepper, marker pop, sync ring, notification slide, and success check. Every primitive checks reduced-motion preference and has a static equivalent. See `MOTION_SYSTEM.md` for timings.
