# Build-time: where the Regrade API is hosted (no trailing slash).
# Required for Firebase Hosting (or any static host) because /api is not available there.
# Public HTTPS API used by the website app and desktop installers.
VITE_API_BASE_URL=https://api.regradeapp.tech
